# TODO - Kiểm tra & ghép file dự án

- [ ] Kiểm tra BTL ATBMTT (Program.cs, Models, Services) xem có lỗi build/logic và trùng lặp.
- [ ] Soát cấu trúc và nội dung các file trong folder `app/` và `BTL ATBMTT/` xem có trùng lặp/đè lẫn namespace/kết nối DB.
- [ ] Lập kế hoạch ghép các file về cùng một folder `bai tap lon` (theo cấu trúc hợp lý), tránh ghi đè file hiện có.
- [ ] Nếu có thể sửa (lỗi cú pháp/namespace/thiếu file tham chiếu), thực hiện chỉnh sửa.
- [ ] Chạy build/test (dotnet nếu có .csproj; hoặc chạy thử scripts nếu có package.json / python).
- [ ] Cập nhật lại báo cáo kết quả (file nào đã sửa, lý do sửa, cách chạy).

