# TODO

- [ ] Cấu hình & chạy FastAPI (tạo venv, pip install, uvicorn app.main:app --reload)
- [ ] Kiểm tra/khởi tạo database SQLite (app.db) và endpoint /docs hoạt động
- [ ] Chạy benchmark .NET (dotnet build/run SecurityBenchmark.csproj)
- [ ] Kiểm tra Output/benchmark.csv và Output/benchmark.json được tạo
- [ ] Ghi nhận kết quả chạy (console + file Output)

